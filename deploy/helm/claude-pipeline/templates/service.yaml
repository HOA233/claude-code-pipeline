apiVersion: v1
kind: Service
metadata:
  name: {{ include "claude-pipeline.fullname" . }}
  labels:
    {{- include "claude-pipeline.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "claude-pipeline.selectorLabels" . | nindent 4 }}